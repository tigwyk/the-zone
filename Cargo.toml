[package]
name = "the-zone"
version = "0.1.0"
edition = "2024"

[dependencies]
# "wav" so the cues in assets/audio decode; Bevy ships vorbis only by default.
bevy = { version = "=0.19.1", features = ["wav"] }
ron = "0.12"
serde = { version = "1", features = ["derive"] }
